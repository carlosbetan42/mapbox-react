import reactLogo from "../logo.svg";

const ReactLogo = () => {
  return <img src={reactLogo} alt='React Logo' style={{ position: "fixed", bottom: "25px", right: 0, width: "60px" }} />;
};

export default ReactLogo;
